<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.IoM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/cormorant_bold</item>
        <item name="android:textColor">@color/purple</item>
    </style>

    <style name="Theme.IoM" parent="Base.Theme.IoM" />
    <style name="TextAppearance.UID" parent="TextAppearance.MaterialComponents.Headline1">
        <item name="android:fontFamily">@font/cormorant_sc_bold</item>
        <item name="android:textSize">20sp</item>
        <item name="android:padding">8dp</item>
    </style>
</resources>